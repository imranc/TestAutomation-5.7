*** Settings ***

Documentation  A test case to add a youtube link in an entry 

Resource       common_resource.txt


*** Test Cases ***

Create an entry with a youtube link
	Close All Browsers
	Login To Escenic Live Center
	Open an Event
	Create Entry  Test entry of Escenic event with a youtube link
        Add youtube link to an Entry
	Click Element  xpath=//div/a[text()='Publish']
	Wait Until Page Contains Element  xpath=//div/p[contains(text(),'Test entry of Escenic event with a youtube link')]  30
    	Page Should Contain Element  xpath=//div/p[contains(text(),'Test entry of Escenic event with a youtube link')]

Log off
    Log Off From Escenic Live Center