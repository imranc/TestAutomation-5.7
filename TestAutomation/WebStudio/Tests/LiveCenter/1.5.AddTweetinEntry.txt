*** Settings ***

Documentation  A test case to add a tweet in an entry 

Resource       common_resource.txt


*** Test Cases ***

Create an entry with a tweet
	Close All Browsers
	Login To Escenic Live Center
	Open an Event
	Create Entry  Test entry of Escenic event with a tweet
        Add Tweet to an Entry
	Publish Entry
	Wait For  2
	Page Should Contain Element  xpath=//div/p[text()=' Test entry of Escenic event with a tweet']
	Open live center publication event page
	Select Frame  css=iframe[class="twitter-tweet twitter-tweet-rendered"]
	Page Should Contain Element  css=a[href^="https://twitter.com/"]
	Page Should Contain Element  css=a[class="FollowButton follow-button profile"]

