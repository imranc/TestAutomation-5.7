*** Settings ***

Documentation  A test case to add an instagram link in an entry 

Resource       common_resource.txt


*** Test Cases ***

Create an entry with an instagram link
	Close All Browsers
	Login To Escenic Live Center
	Open an Event
	Create Entry  Test entry of Escenic event with an instagram link
	Add instagram link to an Entry
	Click Element  xpath=//div/a[text()='Publish']
	Wait Until Page Contains Element  xpath=//div/p[contains(text(),'Test entry of Escenic event with an instagram link')]  10
    	Page Should Contain Element  xpath=//div/p[contains(text(),'Test entry of Escenic event with an instagram link')]
	Open live center publication event page
	Page Should Contain Element  xpath=//h2[contains(text(),'Test entry of Escenic event with an instagram link')]
	Close All Browsers