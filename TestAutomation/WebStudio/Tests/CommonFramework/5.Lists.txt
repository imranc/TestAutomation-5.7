*** Settings ***

Documentation  A test case to open lists and add contents to list

Resource       common_resource.txt


*** Test Cases***

Login to CUE
    Login To CUE
    
Open list
    Click Element  css=#panels .tabs > .lists
    Click Element  xpath=//li[contains(text(), "NewsList")]
    Page Should Contain  Location: New Articles
    Page Should Contain Element  xpath=//div[contains(text(),"NewsList")]

Add content to list from search panel
    Click Element  css=#panels .tabs > .search
    Input Text  css=input[name="search"]  Catchy
    Drag And Drop With Sikuli  PublishedStoryInSearchResults  NewsListDroppablePanel
    Alert Should Not Be Present
    Page Should Contain Element  css=div.list-item
    Click Element  css=#cf-list-editor-loader .remove
    Page Should Not Contain Element  css=div.list-item

Create content and add that to list
    Create Picture Content  Image content created by automated tests
    Publish Picture
    Click Element  css=#panels .tabs > .lists
    Click Element  xpath=//li[contains(text(), "NewsList")]
    Page Should Contain  Location: New Articles
    Page Should Contain Element  xpath=//div[contains(text(),"NewsList")]
    Drag And Drop With Sikuli  ImageContentTab  NewsListDroppablePanel
    Alert Should Not Be Present
    Page Should Contain Element  css=div.list-item
    Click Element  css=#cf-list-editor-loader .remove
    Page Should Not Contain Element  css=div.list-item
    
Log off
    Log Off From CUE
    Close Browser