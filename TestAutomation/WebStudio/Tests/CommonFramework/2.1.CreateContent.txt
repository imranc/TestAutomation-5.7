*** Settings ***

Documentation  A test case to Create content in CUE


Resource       common_resource.txt


*** Test Cases ***

Login to CUE
    Login To CUE
    
Create Picture
    Switch To CUE
    Create Picture Content  Image content created by automated tests
    Publish Story
    Wait Until Angular Ready   15s
    Wait Until Element Is Visible  css=div[ng-if="vm.content.links && vm.content.links['alternate']"]>div[class="right"]  15
    ${previewUrl}=  Get Element Attribute  css=a[ng-href*="http"]@ng-href
    Log Off From CUE
    Close Browser
    Open Browser  ${previewUrl}  ${BROWSER}
    Maximize Browser Window
    Wait Until Element Is Visible  css=img[src*="ALTERNATES"]
    Page Should Contain Element  css=img[src*="ALTERNATES"]
    Close Browser

